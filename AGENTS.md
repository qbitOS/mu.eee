# Agent context — mu.eee (μ'search shell)

You are working in **mu.eee**: the **compliance and reference** pack for the **μ'search** unified shell (**`mueee.html`**).

## Rules

1. **Read first:** `README.md`, `COMPLIANCE.qmd`, `docs/BUILD.md`.
2. **Do not fork iframe apps** — Search, kbatch, etc. stay in [qbitOS/uvspeed](https://github.com/qbitOS/uvspeed) `web/`.
3. **Spine order** — Normative order is defined in `web/mueee-throughline-spine.js` (`ORDER` array); change upstream only.
4. **This repo** is documentation + compliance + CI verification — not a second copy of `web/mueee.html` unless you add an explicit mirror policy.

## Edits here vs uvspeed

- **Product changes** → **uvspeed** `mueee.html` / `build_mueee.py` / `mueee-throughline-spine.js`.
- **This repo** → compliance text, `reference/mu-eee-manifest.json`, workflow tweaks.
